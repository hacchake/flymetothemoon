# FLY ME TO THE MOON — Architecture

> FlyWire の配線を素材にした小さな脳を持つハエが、夜の街を飛ぶ。
> 人間には「月を目指している」ように見えるが、ハエの中にあるのは光と発火と翅の動きだけ。
>
> 更新：2026-09-18。Phase 0–5 を実装。Phase 6（ゲームの完成度）は途中、Phase 7（公開）はほぼ整っている。

---

## 0. 方針：科学的忠実度と、作品としての面白さを分ける

| 層 | ファイル | 中身 | 忠実度 |
|---|---|---|---|
| **科学** | `src/circuit_flywire.js`（生成物）, `src/circuit.js`, `src/brain.js` | ニューロン、シナプス、LIF | 視覚と逃避は **FlyWire v783 の実ニューロン・実シナプス数**。LIF は Shiu et al. 2024 と同じ。嗅覚だけ模式 |
| **接点** | `src/interface.js` | 世界 → 感覚ニューロンの入力率、DN の発火率 → 翅 | 手で決めた変換。connectome からは出てこない |
| **ゲーム** | `src/world.js`, `src/game.js`, `src/stages.js` | 夜の街、罠、ステージ、スコア | 大胆に単純化 |
| **表現** | `src/render.js`, `src/audio.js`, `index.html` | 絵、脳の可視化、音楽 | 人間に見せる層。ここで初めて光のひとつが「月」として描かれる |

**「月」という概念は科学層にも接点層にも無い。** 接点が受け取るのは光の方位・大きさ・明るさだけで、
月・街灯・殺虫灯・ヘッドライト・ランタンを区別しない（殺虫灯は、ハエにとってだけ特別に明るい）。

---

## 1. 技術の選択：JavaScript + Canvas + Web Audio（依存ゼロ）

- `index.html` をダブルクリックするだけで動く（`file://` でも可）。ビルドも外部ライブラリも無い。
- Python は、FlyWire データから回路を抜き出すオフライン処理（`tools/`）だけに使う。

---

## 2. 規模と速さ（この PC：Ryzen 5 5560U、GPU なし）

| 回路 | ニューロン | 結合 | 生物時間 1 秒あたりの計算時間 | 1 フレームあたり |
|---|---|---|---|---|
| FlyWire（既定） | 3,786 | 40,854 | 約 235 ms | 約 3.9 ms |
| 模式（比較用） | 244 | 約 1,600 | 約 20 ms | 約 0.3 ms |

描画と音を含めても 60 fps の予算（16.7 ms）に収まる。

---

## 3. データの流れ（1 フレーム = 1/60 秒）

```
 プレイヤー（ランタン・餌・雲・救出の連打）
        │
        ▼
 World（夜の街：月・街灯・殺虫灯・車・ハエトリ紙・ハエ叩き・雲・餌）
        │ 光源の方位/大きさ/明るさ、迫る影の大きさと広がる速さ、左右の触角の匂い
        ▼
 Senses ── modality ごとに Poisson 率 ──▶ Brain（LIF、0.5 ms × 33 刻み）
                                          │ DNa02 左右・DNp09・MDN・GF の発火率
                                          ▼
 World ◀── 速さ・旋回・逃避の突進 ── Motor
        │ 出来事（食べた・捕まった・焼かれた・叩かれた・月に届いた）
        ▼
 Game（ステージ・3 匹・スコア・★） ── 空腹 ──▶ Senses（Or42b の感度）
        │
        ├─▶ Render（夜の街 / 脳：FlyWire 座標の点・シナプスを流れる光・GF の衝撃波・複眼）
        └─▶ Jazz（発火の量・左右・推力・GF・月との距離 → 音数・旋律・フィルタ・残響）
```

---

## 4. FlyWire から回路を抜き出す（Phase 3、実施済み）

### 4.1 データ（ログイン不要、約 136 MB、`data/` は git に入れない）

| ファイル | 中身 |
|---|---|
| `Connectivity_783.parquet`（Shiu et al.） | 15,091,983 本の有向結合。符号付きシナプス数 |
| `Completeness_783.csv` | 138,639 ニューロンの並び |
| `neuron_annotations.tsv`（Schlegel et al.） | 139,248 ニューロンの細胞型・左右・位置 |

### 4.2 どの視覚ニューロンを入力にするか：データに選ばせた（`tools/survey_steering.py`）

各細胞型 × 左右について、1〜3 段の経路で DNa02/DNa01（左右）と DNp09 に与える影響を
（受け手の全入力で割った重みの積として）合計した。

| 結果 | 細胞型 |
|---|---|
| 同じ側の DNa02/01 を最も強く動かす視覚投射 | **LC10d, LC10a, LLPC1, LC10c-2, LC10c-1**（LC10 は求愛中にメスを追いかける経路として知られる） |
| DNp09（前進）を最も強く動かす視覚投射 | **LC9, LC31a** |
| GF（DNp01）へ強く届く | **LPLC2, LC4**（既知の接近・逃避経路と一致） |
| 嗅覚受容体 ORN（DM1 など） → DN | 5 段たどってもほとんど届かない。間に立つのは触角葉の投射ニューロン、キノコ体、側角、LAL（旋回中枢） |
| 触角の機械感覚 JO-B（音・空気のふるえ） → DN | **GF（DNp01）に最も強く届く**（0.015）。既知の多感覚の逃避経路と一致 |
| JO-C/E（風・重力） → DN | GF（0.006）と DNa02・DNp09 に届く |
| DNg02（飛行の推力として知られる） | v783 の脳の注釈に見当たらない → 推力は DNp09 で代用（仮定） |

### 4.3 抜き出し（`tools/extract_circuit.py`）

1. 入力（1,886 ニューロン）
   - 光・旋回：LC10a/c-1/c-2/d ／ 光・前進：LC9/LC31a ／ 接近：LPLC2/LC4
   - 匂い：ORN_DM1/DM2/DM4/VM2（酢・発酵＝誘引）、ORN_V（二酸化炭素＝忌避）
   - 風：JO-C/E 群 ／ 音・空気のふるえ：JO-B 群
2. 出力：DNa02, DNa01, DNp09, MDN, DNp01（GF）。合わせて 12 ニューロン。
3. 中間（1,876 ニューロン）：「入力からの影響 × DN への影響（後ろ向き 3 段）」で順位をつけ、
   感覚の系統ごとに枠を分ける（視覚 900・嗅覚 500・機械感覚 500）。嗅覚は経路が長いので前向き 4 段までたどる。
   選ぶ単位は**細胞型**（1 個ずつ選ぶと左右どちらかに偏り、匂いを右に置いても左にしか曲がらない脳になった）。
   1 つの型から取るのは最大 48 個、左右で半分ずつ。
4. 選んだニューロンどうしの結合のうち、シナプス数 5 以上のものを実数のまま使う → 40,836 結合、約 65 万シナプス。
5. 左右の対称化：コネクトームは 1 匹の脳なので左右が完全には対称でない。80 シナプス以上の強い投射で
   鏡像の相手がいないもの 17 本にかぎり、鏡像側にも同じ結合を作る（`prov: "mirrored"`）。
   これをしないと、AOTU019_L → DNa02_R の 216 シナプスの抑制だけが片側に効き、旋回が張りついた。
6. 位置（正面から見た x・y と立体の X・Y・Z）も書き出し、脳の可視化に使う。全脳 40,000 点の背景（`brain_atlas.js`）も作る。

### 4.4 実データの回路で確かめたこと（`tests.html`）

| 試験 | 結果 |
|---|---|
| 左目に光（開ループ） | DNa02 左 213 Hz / 右 0 Hz |
| 右目に光 | DNa02 左 0 Hz / 右 164 Hz |
| 左から迫る影 | GF 365 Hz |
| 刺激なし | GF 0 Hz |
| 月に届く（左前方 / 真後ろ） | 無傷 4/4・4/4 → 光の入力をすべて切ると 2/4・1/4 |
| LC10 だけを残す | 4/4（LC10 だけで足りる。LC9/LC31a の経路も単独で足りる＝冗長） |
| 餌の匂いをたどる | 無傷 7/12 → 匂いを感じなくすると 4/12（残る 4 回はさまようあいだの偶然） |
| ハエ叩きから 20 秒生き延びる | 無傷 4/4 → LPLC2・LC4 を切ると 2/4 |
| 接近検出を切って、触角の風と音だけ | 0/4。JO も切っても 0/4 ＝ **触角だけでは逃げられない** |
| 弱い接近（GF 97 Hz）に空気のふるえを重ねる | GF 121 Hz。閾値 100 を越える ＝ 音は最後の一押しとして効く |

**匂いをたどる行動だけは、回路からは出てこない。** 触角の間隔は 0.5 mm しかなく、左右の差では匂い源へ向かえない
（実際の ORN の左右差 140 Hz / 40 Hz は、投射ニューロンでは 307 Hz / 280 Hz まで圧縮される）。
そこで実際のハエと同じく、濃さの「変化」を読む接点層の変換を置いた
（濃くなる間は曲がらない＝ surge、薄れたら左右に振る＝ casting、源の近くでは減速）。ここは実データの手柄ではない。

**光のほうへ曲がる性質は、FlyWire の実際の配線から出てきた**（入力の選び方にはデータにもとづく選択が入っている）。
DNa01 は、この回路では左右の関係がはっきりしなかったので、旋回の読み出しには使っていない。

### 4.5 暗闇での自発発火と、左右の非対称

- 最初は、暗闇でも視覚投射ニューロン 1,153 個が約 8 Hz で発火する設定にしていた。これが抜き出した回路を通って
  右の DNa02 を常に 20〜40 Hz 動かし、ハエは右へ曲がるくせを持っていた。光のような強い刺激には勝てるが、
  匂いのような弱い左右差は埋もれ、餌を 1/12 しか見つけられなかった。
- 暗闇での自発発火を 0.5 Hz、背景の明るさを 0.003 に下げると、偏りは約 3 Hz まで減り、餌は 12/12 になった。
- 残った偏りは、接点層の「零点合わせ」（`Motor.calibrate`：暗闇で DNa02 の左右差を測って差し引く）で打ち消している。
- 嗅覚受容体には順応を入れた（慣れた濃さからの比だけが発火になる）。これで左右の差と、近づくにつれ濃くなる変化が残る。

### 4.6 光だけで Giant Fiber が発火する

抜き出した回路には、右目の前進用視覚（LC9 / LC31a）から GF（DNp01）へ届く経路がある。
右目だけに強い光を当てると GF は約 40 Hz、明るい街灯のそばでは 90 Hz 近くまで発火する（左目・両目ではほとんど発火しない）。
一方、迫る影（ハエ叩き・カエル）では GF は 200〜400 Hz になる。

逃避の閾値を 25 Hz にしていた頃は、何もない空で急に突進し、シンバルが鳴っていた（プレイヤーから「ピシッと鳴って方向転換する」と指摘された）。
閾値を 100 Hz にすると、光による突進は 0 回（街灯 2 本の横を飛ぶ試験で 10 回 → 0 回）、ハエ叩きから生き延びる率は 4/4 のまま。
回路はそのままにしてある（この左右非対称は実データの性質）。

---

## 5. ゲーム（Phase 4・6）

- 横から見た夜の街。幅 1000、高さはステージごと（1,300〜3,000）。カメラはハエを追って縦にスクロールする。
- 12 ステージ + 自由実験 + ステージエディタ。ハエは 3 匹。★ = 月に届く / 1 匹も失わない / 目標時間以内。進み具合はブラウザに保存する。
- 罠と、それがハエのどの感覚に効くか：

| 罠・光 | 効く感覚 | 結果 |
|---|---|---|
| 街灯 | 光（近いほど月より明るい） | 周りを回って力尽きる |
| 殺虫灯 | 光（紫外線：ハエにだけ強い） | 触れると焼かれる |
| 車 | ヘッドライト（前方だけを照らす光） + 車体の接近 | はねられる |
| 飛行機 | ストロボ（一瞬だけ強い光） | 進路がそれる |
| UFO | 光と、ときどきのビーム | ビームに入ると吸い上げられ、どこかへ降ろされる |
| ホタル | 点滅する光 | 進路がそれる |
| ハエトリ紙 | 匂い（甘い） | 捕まる。連打で救出 |
| 酢トラップ | 匂い（強い） | 沈む。連打で救出 |
| クモの巣 | なし（見えにくい） | 捕まる。クモが近づく。連打で救出 |
| ハエ叩き | 迫る影（LPLC2 → GF） | 逃げ遅れると叩かれる |
| カエル | 頭をもたげる影（LPLC2 → GF） | 舌に捕まる |
| 雲 | 光を遮る・接近 | 道具にも障害物にもなる |
| 魔女（まれ） | 小さな灯り | 会えると +777、記録に残る |

プレイヤーの道具：ランタン（電池制）、餌、雲、扇風機（上向きの風）、鏡（月の光を映す）。
捕まって助け出された直後の 1.5 秒は、同じ種類の罠にかからない（すぐ同じ巣に戻ってしまうのを防ぐ）。

ステージエディタ：スタート・月・罠を置き、高さ・道路・飛行機・魔女・曲・道具の数を決める。
つくったステージはブラウザに保存し、「共有 URL」でステージ全体を URL の `#s=` に入れて渡せる（サーバ不要）。

- 手助けなしでの難易度（スクリプトで確認）：ステージ 1・5・7 は自力でクリアすることがある。3・4・9 は手助けなしではほぼ全滅。
  ランタン・雲・餌・救出の連打を使うスクリプトなら、2・3・4・9・10・12 はクリアできることを確かめた（12 は 3 回に 1 回ほど）。

---

## 6. 表現（Phase 5）

- **脳**：FlyWire の実座標にニューロンを置く。背景は全脳 6,000 点。発火したニューロンから、実際のシナプスに沿って光の粒が流れる（金 = 興奮、青 = 抑制）。
  DN は輪で示し、GF が発火すると衝撃波が走る。左右の六角形は、ハエの複眼に映っている世界そのもの。
- **音楽**：オリジナル 7 曲（スウィング、バラード、ボサノバ、ラウンジ、ビバップ、ワルツ、短調の夜想曲）。ステージごとに曲が変わる。
  発火の量 → ビブラフォンの音数・フィルタ・ディレイ・リバーブ、DNa02 の左右 → 旋律の上下と定位、GF の逃避 → クラッシュ、
  ハエの高度 → 旋律の音域とパッドの明るさ、月との距離 → パッド、危なさ → パッドの濁りと低い鼓動、
  カーソルの速さ → キラキラした音（左右 = 定位、上下 = 音の高さ）、ランタン → 高い和音のトレモロ。
- **ラベルのフラッシュ**：ふだんは発火に応じて明るさが変わるだけ。枠つきで光るのは、逃避・食事・月・死・魔女などの特別な瞬間と、
  その集団だけが平均の 3 倍以上に跳ね上がったときだけ。

---

## 7. 残っていること

- 嗅覚を FlyWire の実データに置き換える（ORN → PN → 側角 → DN、4 段以上の経路を抜き出す）。
- 左右の非対称の扱い（そのまま「くせ」とするか、較正するか）。
- ゲーム：ステージの調整、チュートリアル、スマホでの操作感。
- 可塑性（connectome は変えず、接点層の係数や一部の経路だけを学習させる）は、まだ入れていない。

---

## 参考文献

- Dorkenwald et al. 2024, *Neuronal wiring diagram of an adult brain*, Nature（FlyWire）
- Schlegel et al. 2024, *Whole-brain annotation and multi-connectome cell typing of Drosophila*, Nature
- Shiu et al. 2024, *A Drosophila computational brain model reveals sensorimotor processing*, Nature
- Rayshubskiy et al. 2020, *Neural control of steering in walking Drosophila*, bioRxiv（DNa02）
- Bidaye et al. 2014, Science（MDN）／ Bidaye et al. 2020, Neuron（P9 / DNp09）
- Ache et al. 2019, Current Biology ／ von Reyn et al. 2017（LPLC2・LC4 → Giant Fiber）
- Ribeiro et al. 2018, Cell ／ Hindmarsh Sten et al. 2021, Nature（LC10 と視覚追跡）
- Namiki et al. 2022, eLife（DNg02 と飛行）
- Gaudry et al. 2013, Nature ／ Olsen & Wilson 2008, Nature ／ Root et al. 2011, Cell（嗅覚）
- Eon Systems 2026, *How the Eon Team Produced a Virtual Embodied Fly*
